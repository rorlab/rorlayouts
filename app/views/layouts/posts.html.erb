<% content_for :content do %>
<div class="controller_layout">
  <span class="hint">controller layout</span>
  <%= content_for?(:action_content) ? yield(:action_content) : yield %>
</div>
<% end %>

<%= render :template => "layouts/application" %>