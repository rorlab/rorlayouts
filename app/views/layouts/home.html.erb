<% content_for :content do %>
<div class="container_layout">
  <span class="hint">home controller layout</span>
  <%= content_for?(:home_content) ? yield(:home_content) : yield %>
</div>
<% end %>

<%= render :template => "layouts/application" %>